# The image build only needs the Dockerfile, sample-input/, and the two release
# tarballs the workflow stages into the context under fixed generic names (one
# pair per target platform). Exclude everything else (esp. the huge target/ and
# deps/ trees).
*
!Dockerfile
!sample-input/
!snip36.tar.gz
!snip36-deps.tar.gz
