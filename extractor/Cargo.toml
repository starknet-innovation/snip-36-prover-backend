[package]
name = "virtual-os-extractor"
# Excluded from the workspace (needs deps/sequencer/), so it can't inherit
# workspace.package — keep this in step with the workspace version manually.
version = "1.2.3"
edition = "2021"
description = "Extracts the compiled virtual OS program from apollo_starknet_os_program"

[dependencies]
apollo_starknet_os_program = { path = "../deps/sequencer/crates/apollo_starknet_os_program" }
